const pattern = require('./index');
const request = require('supertest');
const Koa = require('koa');

test('mvc pattern', t => {
    const app = new Koa()
    t.pass()
})

test('rest pattern', t => {
    t.pass()
})

